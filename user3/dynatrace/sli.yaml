spec_version: 0.1.4
indicators:
    service_cpu: metricSelector=builtin:service.cpu.perRequest:splitBy():auto:names
    service_rt: entitySelector=type(SERVICE),entityName("BookingService")&metricSelector=builtin:service.response.server:splitBy("dt.entity.service"):auto:names
